<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">

    <Properties>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</Property>
        <Property name="LOG_DIR">C:/Users/d_aad/AppData/Local/alphagnfss/PianoController/0.1.0/log</Property>
    </Properties>

    <Appenders>
        <!-- Console appender outputs logs to console -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>

        <!-- RollingFile appender writes logs to files and rolls them -->
        <RollingFile name="FileAppender"
                     fileName="${LOG_DIR}/pianoController.log"
                     filePattern="${LOG_DIR}/pianoController-%d{yyyy-MM-dd}-%i.log.gz"
        >
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>
            <DefaultRolloverStrategy max="7"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- Root logger logs ERROR and above to console and file -->
        <Root level="error">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileAppender"/>
        </Root>

        <!-- Logger for your application package at DEBUG level -->
        <Logger name="com.alphagen.studio.pianocontroller" level="info" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileAppender"/>
        </Logger>
    </Loggers>

</Configuration>
